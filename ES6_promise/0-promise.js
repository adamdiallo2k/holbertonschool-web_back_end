function getResponseFromAPI() {
    return new Promise((/* eslint-disable no-unused-vars */ resolve, reject /* eslint-enable no-unused-vars */) => {
    });
  }
  
  export default getResponseFromAPI;
