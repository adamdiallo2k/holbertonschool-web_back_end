#!/usr/bin/env python3
""" commented module """
from typing import List, Union, Tuple


def sum_mixed_list(input_list: List[Union[int, float]]) ->  tuple(str, float):
    """ Takes a list of floats as argument
    and returns the sum of the floats. """
    return tuple(input_list)
